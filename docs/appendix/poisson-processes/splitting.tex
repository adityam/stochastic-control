\usebodyfont[termes,10pt]
\starttext
\startMPpage[offset=1mm]
  autoarrows := true;
  ahfactor := 5;

  u := 1cm;

  z[0] = origin;
  z[1] = (2u,0);
  z[2] = (3u,0);

  z[10] = (x[0], -u);
  z[11] = (x[1], -u);
  z[12] = (x[2], -u);

  path p[];

  p[0]  := fullcircle scaled (0.25u) shifted z[0];
  p[10] := fullcircle scaled (0.25u) shifted z[10];

  p[1]  := fullcircle scaled (0.25u) shifted z[1];
  p[11] := fullcircle scaled (0.25u) shifted z[11];

  draw p[0];
  draw p[10];

  draw p[1];
  draw p[11];

  drawarrow (x[0]  - u, y[0] ) -- z[0]  cutafter p[0];
  drawarrow (x[10] - u, y[10]) -- z[10] cutafter p[10];

  path q[];

  q[0]  := z[0]  -- z[1]  cutbefore p[0]  cutafter p[1];
  q[10] := z[10] -- z[11] cutbefore p[10] cutafter p[11];

  q[1]  := z[0]  -- z[11] cutbefore p[0]  cutafter p[11];
  q[11] := z[10] -- z[1]  cutbefore p[10] cutafter p[1];

  drawarrow q[0];
  drawarrow q[1];
  drawarrow q[10];
  drawarrow q[11];

  drawarrow z[1]  -- z[2]  cutbefore p[1];
  drawarrow z[11] -- z[12] cutbefore p[11];
  

  label.lft("$λ_1$", (x[0] - u, y[0]));
  label.lft("$λ_2$", (x[10] - u, y[10]));

  label.rt("$μ_1$", z[2]);
  label.rt("$μ_2$", z[12]);

  label("$+$", z[1]);
  label("$+$", z[11]);

  label.top("\switchtobodyfont[6pt] $0.25$", point 0.2 along q[0]);
  label.bot("\switchtobodyfont[6pt] $0.25$", point 0.2 along q[10]);

  labeloffset := 0pt;

  label.llft("\switchtobodyfont[6pt] $0.75$", point 0.2 along q[1]);
  label.ulft("\switchtobodyfont[6pt] $0.75$", point 0.2 along q[11]);

\stopMPpage
\stoptext
