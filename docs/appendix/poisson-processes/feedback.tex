\usebodyfont[termes,10pt]
\starttext
\startMPpage[offset=1mm]
  autoarrows := true;
  ahfactor := 5;

  u := 1cm;

  z[0] = origin;
  z[1] = (2u,0);
  z[2] = (3u,0);

  z[10] = (x[1], y[1] - u);

  path p[];

  p[0] := fullcircle scaled (0.25u) shifted z[0];
  p[1] := fullcircle scaled (0.25u) shifted z[1];

  draw p[0];
  draw p[1];

  drawarrow (x[0]  - u, y[0] ) -- z[0]  cutafter p[0];

  path q[];

  q[0]  := z[0]  -- z[1]  cutbefore p[0]  cutafter p[1];
  q[1] := z[1] -- z[10] -- (x[0], y[10]) -- z[0] cutbefore p[1] cutafter p[0];
  q[2] := z[1]  -- z[2]  cutbefore p[1];

  drawarrow q[0];
  drawarrow q[1];
  drawarrow q[2];


  

  label.lft("$λ$", (x[0] - u, y[0]));

  label.rt("$μ$", z[2]);

  label("$+$", z[0]);


  label.top("\switchtobodyfont[6pt] $0.75$", point 0.2 along q[2]);

  labeloffset := 0pt;
  label.rt("\switchtobodyfont[6pt] $0.25$",  point 0.1 along q[1]);


\stopMPpage
\stoptext
